@model PresentationLayer.Models.RegisterViewModel
@{
    ViewData["Title"] = "Register Page";
}

<div id="layoutAuthentication">
    <div id="layoutAuthentication_content">
        <main>
            <div class="container">
                <div class="row justify-content-center">
                    <div class="col-lg-7">
                        <div class="card shadow-lg border-0 rounded-lg mt-5">
                            <div class="card-header">
                                <h3 class="text-center font-weight-light my-4">Hesap Oluştur</h3>
                            </div>
                            <div class="card-body">
                                <form id="registerForm">
                                    @Html.AntiForgeryToken()

                                    <div class="row mb-3">
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3 mb-md-0">
                                                <input asp-for="Name" class="form-control" placeholder="Ad Soyad" />
                                                <label>Ad Soyad</label>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-floating">
                                                <select asp-for="UserType" class="form-select" id="userType">
                                                    <option value="1">Gerçek kişi</option>
                                                    <option value="2">Tüzel kişi</option>
                                                </select>
                                                <label>Kullanıcı Tipi</label>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-3">
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3 mb-md-0">
                                                <input asp-for="IdentityNumber" class="form-control" id="identityNumber" placeholder="TCKN / VKN" maxlength="11" />
                                                <label>TCKN / VKN</label>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3 mb-md-0">
                                                <input asp-for="TaxOffice" class="form-control" placeholder="Vergi Dairesi" />
                                                <label>Vergi Dairesi</label>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="form-floating mb-3">
                                        <input asp-for="Email" class="form-control" placeholder="e-posta@example.com" />
                                        <label>E-posta</label>
                                    </div>

                                    <div class="row mb-3">
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3 mb-md-0">
                                                <input asp-for="Password" class="form-control" type="password" placeholder="Şifre" />
                                                <label>Şifre</label>
                                            </div>
                                        </div>
                                        <div class="col-md-6">
                                            <div class="form-floating mb-3 mb-md-0">
                                                <input class="form-control" type="password" placeholder="Şifreyi doğrula" />
                                                <label>Şifreyi doğrula</label>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="mt-4 mb-0">
                                        <div class="d-grid">
                                            <button type="submit" class="btn btn-primary btn-block">Kaydı Oluştur</button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                            <div class="card-footer text-center py-3">
                                <div class="small"><a asp-controller="Login" asp-action="Index">Hesabın var mı? Giriş yap</a></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

<script>
    document.getElementById("registerForm").addEventListener("submit", async function (e) {
        e.preventDefault();

        // Şifre eşleşme kontrolü
        const password = document.querySelector("[name='Password']").value;
        const confirmPassword = document.querySelector('input[placeholder="Confirm password"]').value;

        if (password !== confirmPassword) {
            Swal.fire({
                icon: 'error',
                title: 'Hata',
                text: 'Şifreler eşleşmiyor!'
            });
            return; // Formu göndermeden çık
        }

        const formData = {
            Name: document.querySelector("[name='Name']").value,
            UserType: parseInt(document.querySelector("[name='UserType']").value),
            IdentityNumber: document.querySelector("[name='IdentityNumber']").value,
            TaxOffice: document.querySelector("[name='TaxOffice']").value,
            Email: document.querySelector("[name='Email']").value,
            Password: password
        };

        const token = document.querySelector('input[name="__RequestVerificationToken"]').value;

        const res = await fetch('/Register/Register', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'RequestVerificationToken': token
            },
            body: JSON.stringify(formData)
        });

        const data = await res.json();

        if (data.success) {
            Swal.fire({
                icon: 'success',
                title: 'Başarılı',
                text: data.message
            }).then(() => {
                document.getElementById("registerForm").reset();
            });
        } else {
            Swal.fire({
                icon: 'error',
                title: 'Hata',
                text: data.message
            });
        }
    });
</script>


<script>
    const userTypeSelect = document.getElementById('userType');
    const identityNumberInput = document.getElementById('identityNumber');

    userTypeSelect.addEventListener('change', function () {
        if (this.value === "1") {
            identityNumberInput.maxLength = 11;
            identityNumberInput.placeholder = "11 haneli TCKN giriniz";
        } else if (this.value === "2") {
            identityNumberInput.maxLength = 10;
            identityNumberInput.placeholder = "10 haneli VKN giriniz";
        }
        identityNumberInput.value = "";
    });

    identityNumberInput.addEventListener('input', function () {
        this.value = this.value.replace(/\D/g, '');
        if (this.value.length > this.maxLength) {
            this.value = this.value.slice(0, this.maxLength);
        }
    });
</script>